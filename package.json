{
  "name": "maelstrom",
  "version": "1.0.0",
  "description": "MAELSTROM       =========",
  "main": "index.js",
  "scripts": {
    "clean": "rm -fr dist/",
    "prebuild": "npm run clean && mkdir -p dist/js dist/css dist/img dist/mp3",
    "build": "npm run build:js && npm run build:html && npm run build:css && npm run build:img && npm run build:mp3",
    "build:js": "browserify js/index.js > dist/js/index.js",
    "build:html": "cp *.html dist/",
    "build:css": "cp css/*.css dist/css",
    "build:img": "cp img/* dist/img/",
    "build:mp3": "cp mp3/* dist/mp3/",
    "lint": "jshint js/*.js",
    "test": "jasmine",
    "env": "env",
    "server": "ruby -run -e httpd dist/ -p 9090"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/8bitreverie/maelstrom.git"
  },
  "author": "8bitreverie",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/8bitreverie/maelstrom/issues"
  },
  "homepage": "https://github.com/8bitreverie/maelstrom",
  "devDependencies": {
    "browserify": "latest",
    "jasmine": "latest",
    "jshint": "latest"
  },
  "dependencies": {
    "underscore": "latest"
  }
}
